# Caddy configuration for DESKRED with Basic Auth
# For now using HTTP only - HTTPS with IP addresses can be problematic
# Access via: http://91.98.234.148

:80 {
    # Basic Authentication - works on HTTP
    basic_auth {
        cyber $2a$14$PidYKMQ9PPo8fX.aKAhBB.ZXJsYmHpAbP4gXvxmFZIYOkzk0aL7Wa
    }

    # Proxy to frontend
    reverse_proxy /* frontend:5173

    # Proxy API requests to backend
    reverse_proxy /api/* backend:8000
}
